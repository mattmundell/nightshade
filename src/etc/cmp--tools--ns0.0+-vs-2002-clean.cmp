-*- flush-trailing-whitespace: nil -*-

clean-build.lisp.BAK only present in :src/nightshade-0.0+/src/tools/.
clean-build.lisp only present in :src/nightshade-0.0+/src/tools/.
edload.lisp only present in :src/nightshade-0.0+/src/tools/.
build-world.lisp only present in :src/nightshade-0.0+/src/tools/.
setup.lisp.BAK only present in :src/nightshade-0.0+/src/tools/.
+++ /home/matt/src/nightshade-0.0+/src/tools/comcom.lisp
--- :src/cmucl-2002-08-23-19h00-clean/src/tools/comcom.lisp.
+++ /home/matt/src/nightshade-0.0+/src/tools/setup.lisp
--- :src/cmucl-2002-08-23-19h00-clean/src/tools/setup.lisp.
+++++++++++++++
 			      (format t "~A" condition)
---------------
+++++++++++++++
				(format t "Proceeding...~%")
				(format *error-output* "Proceeding...~%")
				(continue))
			      (format t "Aborting...~%")
---------------
				(format *error-output* "Proceeding...~%")
				(continue))
+++ /home/matt/src/nightshade-0.0+/src/tools/config.lisp
--- :src/cmucl-2002-08-23-19h00-clean/src/tools/config.lisp.
+++++++++++++++
      (load "library:subsystems/ed-library"))
---------------
      (load "library:subsystems/hemlock-library"))
edcom.lisp only present in :src/nightshade-0.0+/src/tools/.
+++ /home/matt/src/nightshade-0.0+/src/tools/worldcom.lisp
--- :src/cmucl-2002-08-23-19h00-clean/src/tools/worldcom.lisp.
+++++++++++++++
;; FIX for reverting to pd loop
(comf "target:code/loop")
(load "target:code/loop")

---------------
+++++++++++++++
;; FIX for first time
(if (find-package "FTP")
    (rename-package "FTP" "FTP" nil)
    (make-package "FTP" :nicknames 'nil :use nil))
(use-package '("LISP") "FTP")

(comf "target:code/ftp")
(comf "target:code/base64")

---------------
(comf "target:code/cmu-site")

build-core only present in :src/nightshade-0.0+/src/tools/.
+++ /home/matt/src/nightshade-0.0+/src/tools/heapanal.lisp
--- :src/cmucl-2002-08-23-19h00-clean/src/tools/heapanal.lisp.
+++ /home/matt/src/nightshade-0.0+/src/tools/worldload.lisp
--- :src/cmucl-2002-08-23-19h00-clean/src/tools/worldload.lisp.
+++++++++++++++
---------------
;;; Make sure the core will start up in the user package.
(lisp::assert-user-package)

+++++++++++++++
(load (open "target:features.lisp"))

;;; Get the version of the core.
;;;
(set '*lisp-implementation-version* (read-line (open "target:VERSION")))

---------------
;;; Get some data on this core.
;;;
(write-string "What is the current lisp-implementation-version? ")
(force-output)
(set '*lisp-implementation-version* (read-line))

+++++++++++++++
(setf (ext:search-list "ed:") '("target:ed/"))
---------------
(setf (ext:search-list "hem:") '("target:hemlock/"))
+++++++++++++++
#-runtime (maybe-byte-load "code:ftp")
---------------
+++++++++++++++
#-runtime (maybe-byte-load "code:base64")

---------------
+++++++++++++++
;;; Editor.
;;;
#-(or no-hemlock runtime)
(maybe-byte-load "target:ed/ed-library")

---------------
;;; PCL.
;;;
#-(or no-pcl runtime) (maybe-byte-load "pcl:pclload")

;;; CLX.
;;;
#-(or no-clx runtime)
(maybe-byte-load "clx:clx-library")

;;; Hemlock.
;;;
#-(or no-hemlock runtime)
(maybe-byte-load "target:hemlock/hemlock-library")

;;; CLM.
;;;
#-(or no-clm runtime)
(maybe-byte-load "target:interface/clm-library")

+++++++++++++++
;;;
(setf (search-list "modules:") '("./"))
(setf (search-list "target:") *target-sl*)

;;; Okay, build the core!
---------------
;;; 
(setf (search-list "modules:") '("./"))
(setf (search-list "target:") *target-sl*)

;;; Okay, build the thing!
+++++++++++++++
  ;;
---------------
  ;; 
+++++++++++++++
;   ;; Maybe enable ANSI defstruct :print-function/:print-object processing
;   #-NO-PCL
;   (setq ext:*ansi-defstruct-options-p* t)
---------------
  ;; Maybe enable ANSI defstruct :print-function/:print-object processing
  #-NO-PCL
  (setq ext:*ansi-defstruct-options-p* t)
+++++++++++++++
	  *features*
---------------
	  *features* 
edcom.lisp.BAK only present in :src/nightshade-0.0+/src/tools/.
config.lisp.BAK only present in :src/nightshade-0.0+/src/tools/.
+++ /home/matt/src/nightshade-0.0+/src/tools/mk-release
--- :src/cmucl-2002-08-23-19h00-clean/src/tools/mk-release.
edload.lisp.BAK only present in :src/nightshade-0.0+/src/tools/.
+++ /home/matt/src/nightshade-0.0+/src/tools/worldbuild.lisp
--- :src/cmucl-2002-08-23-19h00-clean/src/tools/worldbuild.lisp.
hemcom.lisp.BAK only present in :src/nightshade-0.0+/src/tools/.
hemload.lisp.BAK only present in :src/nightshade-0.0+/src/tools/.
worldload.lisp.BAK only present in :src/nightshade-0.0+/src/tools/.
worldbuild.lisp.BAK only present in :src/nightshade-0.0+/src/tools/.
worldcom.lisp.BAK only present in :src/nightshade-0.0+/src/tools/.
build-world.lisp.BAK only present in :src/nightshade-0.0+/src/tools/.
build-core.BAK only present in :src/nightshade-0.0+/src/tools/.
